# セキュリティと報告

PseudoSleepは単一のローカルWindowsユーザー向けの画面制御アプリです。画面消灯はWindowsのロックや別ユーザーへの隔離ではありません。Moonlightと物理入力は同じWindowsデスクトップを操作します。

常駐アプリは通常権限で動作します。名前付きパイプは同じユーザーに限定し、名前をセッションごとに分けています。自動切断を有効にする導入では、対象ユーザーにSunshineサービスの状態確認・開始・停止権限を追加します。これにより、そのユーザーはSunshineのすべての配信を停止できます。設定変更権限は追加しません。

Sunshineの管理画面はホスト自身からのアクセスに制限し、UPnPを無効にする設定を導入スクリプトが適用します。VPNやインターネット公開の設定はこのリポジトリでは行いません。Sunshineやドライバ自体の更新・脆弱性情報は各プロジェクトで確認してください。

公開してよい一般的な不具合はGitHub Issueへ報告できます。認証情報、端末ID、未修正の悪用手順は公開Issueに記載しないでください。GitHubの非公開脆弱性報告がリポジトリのSecurity画面で利用できる場合はそちらを使用し、利用できない場合は詳細を伏せて保守者へ連絡方法を確認してください。

[脅威モデル](docs/THREAT_MODEL.md)と[検証記録](docs/deployment-report.md)はレビューの範囲と限界を記録しています。脆弱性が存在しないことや、すべての端末で画面が復元できることを保証するものではありません。
